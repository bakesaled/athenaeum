import { FocusableDirective } from './focusable.directive';

describe('FocusableDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusableDirective({} as any);
    expect(directive).toBeTruthy();
  });
});
